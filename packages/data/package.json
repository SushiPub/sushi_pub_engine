{
  "name": "@socialpublisher/data",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pretest": "ts-node src/db/check-db.ts",
    "test": "npm run migrate && jest",
    "migrate": "knex migrate:latest --knexfile knexfile.ts",
    "rollback": "knex migrate:rollback --knexfile knexfile.ts"},
  "dependencies": {
    "@socialpublisher/core": "*",
    "dotenv": "^17.2.3",
    "pg": "^8.17.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.1.0",
    "jest": "^30.2.0",
    "knex": "^3.1.0",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}