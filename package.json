{
  "name": "socialpublisher",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w @socialpublisher/core",
    "test": "npm test -w @socialpublisher/data"
  }
}